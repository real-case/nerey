{
  "lines": [
    "core[0] files=**/packages/core/src/**/*.{ts,tsx}",
    "core[0] ignores=**/*.spec.{ts,tsx}",
    "core[0] ignores=**/*.stories.tsx",
    "core[0] ignores=**/*.test.{ts,tsx}",
    "core[0] ignores=**/__fixtures__/**",
    "core[0] ignores=**/__tests__/**",
    "core[0] name=nerey/core-stays-headless",
    "core[0] no-restricted-imports group=**/*.css,*.css,@nerey/theme,@nerey/theme/*",
    "core[0] no-restricted-imports group=@base-ui/react,@base-ui/react/*,@radix-ui/*,react-aria*",
    "core[0] no-restricted-imports group=arktype,joi,valibot,yup,zod,zod/*",
    "core[0] no-restricted-imports group=markdown-it,marked,react-markdown,rehype-*,remark-*",
    "core[0] rule no-restricted-imports severity=error",
    "recommended[0] files=**/packages/core/src/**/*.{ts,tsx}",
    "recommended[0] ignores=**/*.spec.{ts,tsx}",
    "recommended[0] ignores=**/*.stories.tsx",
    "recommended[0] ignores=**/*.test.{ts,tsx}",
    "recommended[0] ignores=**/__fixtures__/**",
    "recommended[0] ignores=**/__tests__/**",
    "recommended[0] name=nerey/core-stays-headless",
    "recommended[0] no-restricted-imports group=**/*.css,*.css,@nerey/theme,@nerey/theme/*",
    "recommended[0] no-restricted-imports group=@base-ui/react,@base-ui/react/*,@radix-ui/*,react-aria*",
    "recommended[0] no-restricted-imports group=arktype,joi,valibot,yup,zod,zod/*",
    "recommended[0] no-restricted-imports group=markdown-it,marked,react-markdown,rehype-*,remark-*",
    "recommended[0] rule no-restricted-imports severity=error",
    "recommended[1] files=**/packages/theme/src/**/*.{ts,tsx}",
    "recommended[1] name=nerey/theme-does-not-reexport-base-ui",
    "recommended[1] no-restricted-imports group=@ark-ui/*,@radix-ui/*,react-aria*",
    "recommended[1] rule no-restricted-imports severity=error",
    "recommended[2] files=**/widgets/**/*.{ts,tsx}",
    "recommended[2] name=nerey/widgets-perform-no-io",
    "recommended[2] no-restricted-globals name=WebSocket",
    "recommended[2] no-restricted-globals name=XMLHttpRequest",
    "recommended[2] no-restricted-globals name=fetch",
    "recommended[2] no-restricted-imports group=**/api,**/api/*,**/services/*",
    "recommended[2] no-restricted-imports group=@tanstack/query-core,@tanstack/react-query,swr",
    "recommended[2] no-restricted-imports group=axios,axios/*,got,ky,ofetch,superagent",
    "recommended[2] rule no-restricted-globals severity=error",
    "recommended[2] rule no-restricted-imports severity=error",
    "theme[0] files=**/packages/theme/src/**/*.{ts,tsx}",
    "theme[0] name=nerey/theme-does-not-reexport-base-ui",
    "theme[0] no-restricted-imports group=@ark-ui/*,@radix-ui/*,react-aria*",
    "theme[0] rule no-restricted-imports severity=error",
    "widgets[0] files=**/widgets/**/*.{ts,tsx}",
    "widgets[0] name=nerey/widgets-perform-no-io",
    "widgets[0] no-restricted-globals name=WebSocket",
    "widgets[0] no-restricted-globals name=XMLHttpRequest",
    "widgets[0] no-restricted-globals name=fetch",
    "widgets[0] no-restricted-imports group=**/api,**/api/*,**/services/*",
    "widgets[0] no-restricted-imports group=@tanstack/query-core,@tanstack/react-query,swr",
    "widgets[0] no-restricted-imports group=axios,axios/*,got,ky,ofetch,superagent",
    "widgets[0] rule no-restricted-globals severity=error",
    "widgets[0] rule no-restricted-imports severity=error"
  ],
  "messages": {
    "core[0] no-restricted-imports group=**/*.css,*.css,@nerey/theme,@nerey/theme/*": "@nerey/core ships zero CSS and never depends on the theme. Styling is driven from the data-* contract (ADR 0020); the dependency arrow points theme → core and never back (ADR 0002).",
    "core[0] no-restricted-imports group=@base-ui/react,@base-ui/react/*,@radix-ui/*,react-aria*": "Behavioural primitives belong to @nerey/theme, wrapped and never re-exported (ADR 0022). Core stays headless so a consumer can build their own presentation layer on it.",
    "core[0] no-restricted-imports group=arktype,joi,valibot,yup,zod,zod/*": "Core validates through Standard Schema v1, not through a specific validator — that is what lets a consumer bring their own (ADR 0011). Type against StandardSchemaV1.",
    "core[0] no-restricted-imports group=markdown-it,marked,react-markdown,rehype-*,remark-*": "Core ships no markdown renderer. The fallback renderer is injected through the host value (ADR 0012), so a consumer pays for a markdown pipeline only if they want one.",
    "recommended[0] no-restricted-imports group=**/*.css,*.css,@nerey/theme,@nerey/theme/*": "@nerey/core ships zero CSS and never depends on the theme. Styling is driven from the data-* contract (ADR 0020); the dependency arrow points theme → core and never back (ADR 0002).",
    "recommended[0] no-restricted-imports group=@base-ui/react,@base-ui/react/*,@radix-ui/*,react-aria*": "Behavioural primitives belong to @nerey/theme, wrapped and never re-exported (ADR 0022). Core stays headless so a consumer can build their own presentation layer on it.",
    "recommended[0] no-restricted-imports group=arktype,joi,valibot,yup,zod,zod/*": "Core validates through Standard Schema v1, not through a specific validator — that is what lets a consumer bring their own (ADR 0011). Type against StandardSchemaV1.",
    "recommended[0] no-restricted-imports group=markdown-it,marked,react-markdown,rehype-*,remark-*": "Core ships no markdown renderer. The fallback renderer is injected through the host value (ADR 0012), so a consumer pays for a markdown pipeline only if they want one.",
    "recommended[1] no-restricted-imports group=@ark-ui/*,@radix-ui/*,react-aria*": "The theme has exactly one behavioural dependency — Base UI (ADR 0022). A second means two focus traps, two portals and two Escape implementations, which users notice long before they notice bundle size.",
    "recommended[2] no-restricted-globals name=WebSocket": "Transport belongs to the host, not to a widget (ADR 0015 / 0037). Nerey has no opinion about how messages reach you.",
    "recommended[2] no-restricted-globals name=XMLHttpRequest": "A widget performs no I/O (ADR 0015).",
    "recommended[2] no-restricted-globals name=fetch": "A widget performs no I/O (ADR 0015). Use onInteraction to hand a message to the host, or useWidgetState to persist.",
    "recommended[2] no-restricted-imports group=**/api,**/api/*,**/services/*": "A widget must not reach into an application API layer — that coupling is exactly what makes a widget unportable between hosts (ADR 0015).",
    "recommended[2] no-restricted-imports group=@tanstack/query-core,@tanstack/react-query,swr": "A widget does not talk to the server cache. Persistence flows through the MessagePersistence port the host injects (ADR 0016), which is what lets the same widget run in Storybook against an in-memory implementation.",
    "recommended[2] no-restricted-imports group=axios,axios/*,got,ky,ofetch,superagent": "A widget performs no I/O. Its only outbound channel is onInteraction(action, { text }), and its only persistence channel is useWidgetState (ADR 0014 / 0015 / 0016). Hand the message to the host and let the host own the request.",
    "theme[0] no-restricted-imports group=@ark-ui/*,@radix-ui/*,react-aria*": "The theme has exactly one behavioural dependency — Base UI (ADR 0022). A second means two focus traps, two portals and two Escape implementations, which users notice long before they notice bundle size.",
    "widgets[0] no-restricted-globals name=WebSocket": "Transport belongs to the host, not to a widget (ADR 0015 / 0037). Nerey has no opinion about how messages reach you.",
    "widgets[0] no-restricted-globals name=XMLHttpRequest": "A widget performs no I/O (ADR 0015).",
    "widgets[0] no-restricted-globals name=fetch": "A widget performs no I/O (ADR 0015). Use onInteraction to hand a message to the host, or useWidgetState to persist.",
    "widgets[0] no-restricted-imports group=**/api,**/api/*,**/services/*": "A widget must not reach into an application API layer — that coupling is exactly what makes a widget unportable between hosts (ADR 0015).",
    "widgets[0] no-restricted-imports group=@tanstack/query-core,@tanstack/react-query,swr": "A widget does not talk to the server cache. Persistence flows through the MessagePersistence port the host injects (ADR 0016), which is what lets the same widget run in Storybook against an in-memory implementation.",
    "widgets[0] no-restricted-imports group=axios,axios/*,got,ky,ofetch,superagent": "A widget performs no I/O. Its only outbound channel is onInteraction(action, { text }), and its only persistence channel is useWidgetState (ADR 0014 / 0015 / 0016). Hand the message to the host and let the host own the request."
  }
}
